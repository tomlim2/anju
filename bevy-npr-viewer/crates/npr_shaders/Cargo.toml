[package]
name = "npr_shaders"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18", default-features = false, features = [
    "bevy_asset",
    "bevy_render",
    "bevy_pbr",
] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
